#include "main.h"

/**
 *_abs - computes the absolute value
 *
 *@n: integer to be checked
 *
 *Return: Returns n
 */
int _abs(int n);
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
return (0);
}
